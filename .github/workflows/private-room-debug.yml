name: Private room debug
# insert-path diagnostic

on:
  push:
    branches: [main]

jobs:
  debug:
    if: github.event.head_commit.message == 'Run private-room debug'
    runs-on: ubuntu-latest
    steps:
      - name: Inspect room backend database access
        run: |
          curl -fsS https://hycegznamzjhwinegaai.supabase.co/functions/v1/gomoku-room-debug | jq .
